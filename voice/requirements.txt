# The voice agent, face recognition and voice prints (voice/.venv).
#   cd voice && python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
# Versions are the ones the booth ran on (2026-09). pipecat must stay at
# 1.6.0: later releases renamed an import multilingual.py uses.
pipecat-ai[anthropic,google,mlx-whisper,whisper,kokoro,local-smart-turn,silero,local]==1.6.0
device-connect-edge==0.2.5
insightface==1.0.1
onnxruntime
opencv-python-headless
speechbrain==1.1.1
torch
piper-tts==1.7.0
scipy
soxr
# Linux + NVIDIA only: MLX's CUDA backend, same version as the mlx that
# mlx-whisper pulls in (mlx-cpu on a box without a GPU; nothing on a Mac).
#   .venv/bin/pip install "mlx-cuda-13==$(.venv/bin/pip show mlx | awk '/^Version/{print $2}')"
